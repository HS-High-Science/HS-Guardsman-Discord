image: node:20

stages:
  - build
  - run

compile:
  stage: build
  script:
    - npm ci
    - cp .env.example .env
    - npm run build
  only:
    - master
    - merge_requests

testrun:
  stage: run
  script:
    - npm ci
    - npm run ci --if-present
  artifacts:
    paths:
      - node_modules
  only:
    - master
    - merge_requests
